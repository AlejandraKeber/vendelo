<h1>Productos:</h1>

<% @products.each do |product| %>
  <%= link_to product_path(product), class: 'product' do %>
    <h2><%= product.title %></h2>
    <span>Price: <%= product.price %>$</span>
  <% end %>
<% end %>